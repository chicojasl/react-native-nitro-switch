///
/// HybridNitroSwitchComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridNitroSwitchComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitroswitch::views {

  extern const char HybridNitroSwitchComponentName[] = "NitroSwitch";

  HybridNitroSwitchProps::HybridNitroSwitchProps(const react::PropsParserContext& context,
                                                 const HybridNitroSwitchProps& sourceProps,
                                                 const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    value([&]() -> CachedProp<bool> {
      try {
        const react::RawValue* rawValue = rawProps.at("value", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.value;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<bool>::fromRawValue(*runtime, value, sourceProps.value);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroSwitch.value: ") + exc.what());
      }
    }()),
    onValueChange([&]() -> CachedProp<std::optional<std::function<void(bool /* value */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onValueChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onValueChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(bool /* value */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.onValueChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroSwitch.onValueChange: ") + exc.what());
      }
    }()),
    disabled([&]() -> CachedProp<std::optional<bool>> {
      try {
        const react::RawValue* rawValue = rawProps.at("disabled", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.disabled;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<bool>>::fromRawValue(*runtime, value, sourceProps.disabled);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroSwitch.disabled: ") + exc.what());
      }
    }()),
    thumbColor([&]() -> CachedProp<std::optional<std::variant<double, ColorProp>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("thumbColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.thumbColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::variant<double, ColorProp>>>::fromRawValue(*runtime, value, sourceProps.thumbColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroSwitch.thumbColor: ") + exc.what());
      }
    }()),
    trackColor([&]() -> CachedProp<std::optional<std::variant<double, ColorProp>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("trackColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.trackColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::variant<double, ColorProp>>>::fromRawValue(*runtime, value, sourceProps.trackColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroSwitch.trackColor: ") + exc.what());
      }
    }()),
    trackDecorationColor([&]() -> CachedProp<std::optional<std::variant<double, ColorProp>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("trackDecorationColor", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.trackDecorationColor;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::variant<double, ColorProp>>>::fromRawValue(*runtime, value, sourceProps.trackDecorationColor);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroSwitch.trackDecorationColor: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroSwitchSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroSwitchSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroSwitch.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridNitroSwitchProps::HybridNitroSwitchProps(const HybridNitroSwitchProps& other):
    react::ViewProps(),
    value(other.value),
    onValueChange(other.onValueChange),
    disabled(other.disabled),
    thumbColor(other.thumbColor),
    trackColor(other.trackColor),
    trackDecorationColor(other.trackDecorationColor),
    hybridRef(other.hybridRef) { }

  bool HybridNitroSwitchProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("value"): return true;
      case hashString("onValueChange"): return true;
      case hashString("disabled"): return true;
      case hashString("thumbColor"): return true;
      case hashString("trackColor"): return true;
      case hashString("trackDecorationColor"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridNitroSwitchComponentDescriptor::HybridNitroSwitchComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridNitroSwitchComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                       const std::shared_ptr<const react::Props>& props,
                                                                                       react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridNitroSwitchShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridNitroSwitchComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridNitroSwitchShadowNode&>(shadowNode);
    const HybridNitroSwitchProps& props = concreteShadowNode.getConcreteProps();
    HybridNitroSwitchState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitroswitch::views
