///
/// JHybridNitroSwitchStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "JHybridNitroSwitchStateUpdater.hpp"
#include "views/HybridNitroSwitchComponent.hpp"
#include <NitroModules/NitroDefines.hpp>

namespace margelo::nitro::nitroswitch::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridNitroSwitchState>;

void JHybridNitroSwitchStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridNitroSwitchSpec::javaobject> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> stateWrapperInterface) {
  JHybridNitroSwitchSpec* view = javaView->cthis();

  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapperInterface.get();
  if (!stateWrapperInterface->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) {
      throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapper = jni::alias_ref<react::StateWrapperImpl::javaobject>{
            static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)};

  std::shared_ptr<const react::State> state = stateWrapper->cthis()->getState();
  auto concreteState = std::dynamic_pointer_cast<const ConcreteStateData>(state);
  const HybridNitroSwitchState& data = concreteState->getData();
  const std::optional<HybridNitroSwitchProps>& maybeProps = data.getProps();
  if (!maybeProps.has_value()) {
    // Props aren't set yet!
    throw std::runtime_error("HybridNitroSwitchState's data doesn't contain any props!");
  }
  const HybridNitroSwitchProps& props = maybeProps.value();
  if (props.value.isDirty) {
    view->setValue(props.value.value);
    // TODO: Set isDirty = false
  }
  if (props.onValueChange.isDirty) {
    view->setOnValueChange(props.onValueChange.value);
    // TODO: Set isDirty = false
  }
  if (props.disabled.isDirty) {
    view->setDisabled(props.disabled.value);
    // TODO: Set isDirty = false
  }
  if (props.thumbColor.isDirty) {
    view->setThumbColor(props.thumbColor.value);
    // TODO: Set isDirty = false
  }
  if (props.trackColor.isDirty) {
    view->setTrackColor(props.trackColor.value);
    // TODO: Set isDirty = false
  }
  if (props.trackDecorationColor.isDirty) {
    view->setTrackDecorationColor(props.trackDecorationColor.value);
    // TODO: Set isDirty = false
  }

  // Update hybridRef if it changed
  if (props.hybridRef.isDirty) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = props.hybridRef.value;
    if (maybeFunc.has_value()) {
      std::shared_ptr<JHybridNitroSwitchSpec> shared = javaView->cthis()->shared_cast<JHybridNitroSwitchSpec>();
      maybeFunc.value()(shared);
    }
    // TODO: Set isDirty = false
  }
}

} // namespace margelo::nitro::nitroswitch::views
